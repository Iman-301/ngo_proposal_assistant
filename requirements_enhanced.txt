# Base requirements (same as requirements.txt)
scikit-learn>=1.3
numpy>=1.24
scipy>=1.11

langchain>=0.1.0
langchain-community>=0.0.29
langchain-text-splitters>=0.0.1
langchain-huggingface>=0.0.1
chromadb>=0.4.0         # Vector database
pypdf>=3.0.0            # PDF parsing
python-dotenv>=1.0.0    # For env vars
sentence-transformers>=2.2.2

# Optional: For free LLM support via Ollama (recommended)
# Just install Ollama from https://ollama.com and run: ollama pull llama3.2
# No Python package needed - Ollama runs as a service!

# Optional: For HuggingFace local models (if you have GPU)
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0
